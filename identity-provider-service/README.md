This directory is used by the identity-provider-service container.

When the service has been initialised, it will contain the following directories:
```
identity/
pending/
requests/
revocation/
```

